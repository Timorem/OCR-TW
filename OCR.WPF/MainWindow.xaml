<Window x:Class="OCR.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" DataContext="{Binding ModelView, RelativeSource={RelativeSource Self}}" 
        AllowDrop="True" DragEnter="MainWindow_OnDragEnter" Drop="MainWindow_OnDrop">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Source="{Binding Image, Mode=TwoWay}"/>
        <GroupBox Grid.Row="1" Header="Post processing">
            <StackPanel Orientation="Horizontal">
                <Button Content="Edge detection" Command="{Binding ApplyEdgeDetectionCommand}"/>
                <Button Content="Character isolation" Command="{Binding ApplyCharacterIsolationCommand}"/>

            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
